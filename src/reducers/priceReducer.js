/*import {  ADD_PRICE } from '../actions/types';

const initState = {
    price:'',
    id:''
}


const price = ( state = initState, action ) => {
    switch( action.type ){
        case ADD_PRICE:
            return {
                ...state,
               price:action.pANDid.price,
               id:action.pANDid.id
            };
        default:
            return state;
    }
}

export default price;*/